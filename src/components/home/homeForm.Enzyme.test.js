import expect from 'expect';
import React from 'react';
import {mount, shallow} from 'enzyme';
import TestUtils from 'react-addons-test-utils';
import homForm from './HomePage';

describe('CourseForm via Enzyme', () => {
  it('renders form', () => {
    expect(1).toBe(1);
  });


});
